{
  "name": "ghost-project",
  "version": "1.0.0",
  "description": "A Ghost project with all dependencies resolved.",
  "main": "index.js",
  "dependencies": {
    "ghost": "^5.107.2",
    "knex": "^0.21.21",  // Downgraded knex to 0.21.21 for bookshelf compatibility
    "bookshelf": "^1.2.0", // Compatible with knex@0.21.21
    "@testing-library/react-hooks": "8.0.1",  // Compatible version with React 17
    "react": "17.0.2",   // Downgraded to React 17 for compatibility
    "chai": "5.0.0",     // Updated to satisfy sinon-chai
    "sinon-chai": "4.0.0" // Compatible with chai 5.0.0
  },
  "devDependencies": {
    "sinon": "^12.0.0",   // Compatible sinon version with sinon-chai
    "jest": "^27.0.0",    // Jest for testing
    "webpack": "^5.60.0",  // Ensures Webpack compatibility
    "babel-loader": "^8.2.2" // Ensures Babel transpilation compatibility
  },
  "scripts": {
    "start": "node server.js",  // Adjust based on your entry point
    "build": "npm run build",   // Adjust if necessary for your build toolchain
    "test": "npm run test"      // Placeholder for Jest tests
  },
  "engines": {
    "node": "16.x",  // Node.js version 16 for compatibility
    "npm": "7.x"
  },
  "license": "MIT",
  "resolutions": {
    "knex": "^0.21.21",  // Force resolution of knex version across dependencies
    "chai": "5.0.0"       // Force resolution of chai version
  }
}
